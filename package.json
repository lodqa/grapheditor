{
  "name": "graph-editor",
  "description": "grapd editor",
  "version": "1.0.0",
  "author": "shigeru.nakajima@luxiar.com",
  "dependencies": {
    "stream-browserify": "^1.0.0",
    "xtend": "^4.0.0"
  },
  "devDependencies": {
    "babelify": "^6.0.2",
    "browserify": "^10.1.3",
    "watchify": "^3.2.1"
  },
  "keywords": [
    "graph",
    "edit",
    "jsPlumb"
  ],
  "license": "MIT",
  "main": "index.js",
  "scripts": {
    "start": "browserify index.js -t babelify -o bundle.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "watchify index.js -t babelify -o bundle.js"
  }
}
